<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Date Pitch 😄</title>

<style>
  body {
    margin: 0;
    font-family: Arial, sans-serif;
    background: linear-gradient(135deg, #ff9a9e, #fad0c4);
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    text-align: center;
    overflow: hidden;
  }

  .page {
    display: none;
    font-size: 28px;
    padding: 20px;
    cursor: pointer;
  }

  .active {
    display: block;
  }

  button {
    padding: 12px 25px;
    font-size: 20px;
    margin: 10px;
    border: none;
    border-radius: 8px;
    cursor: pointer;
  }

  #yesBtn {
    background: #4CAF50;
    color: white;
    transition: 0.3s;
  }

  #noBtn {
    background: #f44336;
    color: white;
  }

  .fade-out {
    animation: fade 1.5s forwards;
  }

  @keyframes fade {
    to { opacity: 0; transform: scale(0.8); }
  }
</style>
</head>

<body>

<div id="container">

  <div class="page active">Why you should hire me as your date ?<br><br>(click)</div>

  <div class="page">Reason 1: You are pretty but I can sing about how pretty you are.<br><br>(click)</div>

  <div class="page">Reason 2: You are smart but I can make you smile even at your bad days.<br><br>(click)</div>

  <div class="page">Reason 3: Life is already a movie, but with me it becomes a rom-com with good food and zero boring scenes.<br><br>(click)</div>

  <div class="page" id="finalPage" style="cursor:default;">
    So would you like to go out on a dinner with me ? ❤️<br><br>
    <button id="yesBtn">Yes</button>
    <button id="noBtn">No</button>
    <p id="message"></p>
  </div>

</div>

<script>
  const pages = document.querySelectorAll(".page");
  let current = 0;

  // Page switching
  pages.forEach((page, index) => {
    page.addEventListener("click", () => {
      if(index === current && current < pages.length - 1){
        pages[current].classList.remove("active");
        current++;
        pages[current].classList.add("active");
      }
    });
  });

  // Yes/No logic
  let noClicks = 0;
  const yesBtn = document.getElementById("yesBtn");
  const noBtn = document.getElementById("noBtn");
  const message = document.getElementById("message");
  const container = document.getElementById("container");

  yesBtn.onclick = () => {
    message.innerText = "Yay! Best decision ever 😄🍽️";
  };

  noBtn.onclick = () => {
    noClicks++;
    let size = 20 + (noClicks * 10);
    yesBtn.style.fontSize = size + "px";

    if(noClicks >= 3){
      message.innerText = "ok I will vanish now...";
      container.classList.add("fade-out");
    }
  };
</script>

</body>
</html>
